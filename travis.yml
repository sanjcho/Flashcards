language: ruby
rvm:
  - 2.3.0p0 
env:
  - DB=postgresql
before_install:
  - RAILS_ENV=test
script:
  - bundle exec rake --trace db:migrate test
before_script:
  - psql -c 'create database myapp_test' -U postgres
deploy:
  provider: aws

